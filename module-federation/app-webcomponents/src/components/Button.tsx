import { Button as UIButton } from "ui";

export const Button: React.FC = () => {
  return <UIButton label="Web Components Module Button" />;
};

Button.displayName = "Web Components Module Button";
export default Button;
