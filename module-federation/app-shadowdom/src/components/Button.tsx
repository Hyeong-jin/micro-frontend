import { Button as UIButton } from "ui";

export const Button: React.FC = () => {
  return <UIButton label="ShadowDOM Module Button" />;
};

Button.displayName = "ShadowDOM Module Button";
export default Button;
