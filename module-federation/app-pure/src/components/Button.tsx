import { Button as UIButton } from "ui";

export const Button: React.FC = () => {
  return <UIButton label="Pure Module Button" />;
};

Button.displayName = "Pure Module Button";
export default Button;
